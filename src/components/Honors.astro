---
import { siteConfig } from "../config";
const hasHonors = siteConfig.honors && siteConfig.honors.length > 0;
---

{
  hasHonors && (
    <section id="honors" class="px-6 py-10 sm:px-8 sm:py-12 md:px-12 md:py-14 lg:px-16 lg:py-16">
      <div>
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-16 items-start">
          <div class="lg:col-span-4">
            <h2 class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900">
              Honors & Activities
            </h2>
            <div
              class="w-[75px] h-[5px] mt-2 rounded-full"
              style={`background-color: ${siteConfig.accentColor}`}
            />
          </div>

          <div class="lg:col-span-8">
            <div class="space-y-8">
              {siteConfig.honors.map((item) => (
                <div class="bg-white rounded-lg shadow-sm border border-gray-100 p-4 sm:p-5 md:p-6 hover:shadow-md transition-shadow duration-300">
                  <h3 class="text-lg sm:text-xl font-semibold text-gray-900">
                    {item.category}
                  </h3>
                  {item.intro && (
                    <p class="mt-3 text-sm sm:text-base text-gray-700">{item.intro}</p>
                  )}
                  {item.image && (
                    <div class="mt-4">
                      <img
                        src={item.image}
                        alt={item.imageAlt || `${item.category} photo`}
                        class="w-full max-h-64 object-cover rounded-lg border border-gray-200"
                        loading="lazy"
                      />
                      {item.imageCaption && (
                        <p class="mt-2 text-xs sm:text-sm text-gray-500">
                          {item.imageCaption}
                        </p>
                      )}
                    </div>
                  )}
                  <ul class={`space-y-2 ${item.intro ? "mt-2" : "mt-3"}`}>
                    {item.items.map((entry) => (
                      <li class="flex items-start">
                        <span class="inline-block w-1.5 h-1.5 rounded-full bg-gray-400 mt-2 mr-3 flex-shrink-0" />
                        <span class="text-sm sm:text-base text-gray-600">
                          {entry}
                          {item.itemLinks &&
                            item.itemLinks
                              .filter((itemLink) => itemLink.item === entry)
                              .map((itemLink) => (
                                <>
                                  {" "}
                                  <a
                                    href={itemLink.link}
                                    target="_blank"
                                    rel="noopener noreferrer"
                                    class="font-medium underline underline-offset-4"
                                    style={`color: ${siteConfig.accentColor}`}
                                  >
                                    {itemLink.label}
                                  </a>
                                </>
                              ))}
                          {item.link && item.linkLabel && item.linkItem === entry && (
                            <>
                              {" "}
                              <a
                                href={item.link}
                                target="_blank"
                                rel="noopener noreferrer"
                                class="font-medium underline underline-offset-4"
                                style={`color: ${siteConfig.accentColor}`}
                              >
                                {item.linkLabel}
                              </a>
                            </>
                          )}
                        </span>
                      </li>
                    ))}
                  </ul>
                  {item.link && item.linkItem == null && (
                    <a
                      href={item.link}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="inline-flex items-center mt-4 text-sm font-medium underline underline-offset-4"
                      style={`color: ${siteConfig.accentColor}`}
                    >
                      {item.linkLabel || "View link"}
                    </a>
                  )}
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>
  )
}
